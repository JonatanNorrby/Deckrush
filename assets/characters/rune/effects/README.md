# Rune card-effect animations

This folder contains only Rune's character-specific card effect animations.

Each effect type has its own folder and its own README:

- `magical/`
- `melee/`
- `defensive/`

Do not put PNG files directly in this `effects/` folder. Put them inside the matching effect folder as `1.png`, `2.png`, ... `x.png`.
