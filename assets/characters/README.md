# Character static art

Each character has its own folder.

Current folders:
- `viper/`
- `bastion/`

Inside each character folder the game expects:
- `portrait.png` — main-menu / character-selection portrait.
- `combat.png` — static combat fallback art.

Animated combat frames belong under `../animations/characters/<character-id>/<state>/`, not here.
