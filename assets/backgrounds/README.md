# Background PNGs

Expected filenames:
- `menu.png` — main menu and hero-selection background.
- `combat.png` — combat battlefield background.

Both are optional. CSS backgrounds remain visible when the PNG is missing.
