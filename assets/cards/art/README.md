# Card illustration artwork

Place each card's unique illustration here as:

`<card-id>.png`

Examples: `toxic-cut.png`, `shield-strike.png`, `arcane-bolt.png`, `meteor.png`.

These images fill only the small illustration window on top of the shared base card artwork. Do not include the shared frame, cost, rarity, rules text, or other UI in these images.
