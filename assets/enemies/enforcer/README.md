# Enforcer artwork

`idle/1.png` is the required fallback image for Enforcer. It is used anywhere the game needs a static enemy image and also serves as the first frame of the looping idle animation.

Expected fallback path:

```
assets/enemies/enforcer/idle/1.png
```

A separate `combat.png` fallback is no longer required.

The `idle/`, `attack/`, `damage/`, and `death/` folders contain their own READMEs explaining the exact numbered PNG sequences used by the animation system.
